# Brand assets (vendored)

These two images are the README banner only, copied from the canonical brand kit.

**Source of truth:** [`fiberpath/.github` → `brand/`](https://github.com/fiberpath/.github/tree/main/brand)

Don't edit them here — update the master there, regenerate, and re-copy. The app
icons in `fiberpath_gui/src-tauri/icons/` are likewise generated from that kit's
`icon.png` via `tauri icon`.
